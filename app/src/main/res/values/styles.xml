<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

    <style name="SplashThemes" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@mipmap/ic_splash</item>
    </style>

    <!--通用网页加载页面 进度条样式-->
    <style name="Widget.MaterialProgressBar.ProgressBar.Horizontal" parent="android:Widget.ProgressBar.Horizontal">
        <item name="android:indeterminateDrawable">@null</item>
        <item name="android:minHeight">4dp</item>
        <item name="android:maxHeight">4dp</item>
    </style>

    <style name="text_style0">
        <item name="android:textColor">@color/color_F7FF00</item>
    </style>
    <style name="text_style1">
        <item name="android:textColor">@color/white</item>
    </style>


    <style name="pop_animation_center" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/pop_center_in</item>
        <item name="android:windowExitAnimation">@anim/pop_center_out</item>
    </style>

</resources>
